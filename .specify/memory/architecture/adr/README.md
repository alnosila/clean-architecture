# ADR Index

## How to Use

- Create one file per decision in this directory.
- Use filename format: `YYYY-MM-DD-short-decision-title.md`.
- Keep each ADR concise: context, decision, consequences.

## Decisions

- No ADR documents are recorded yet.
- Recommended first ADR topics based on current code:
  - choice of Clean Architecture layering in a single service;
  - persistence strategy (`pg` + PostgreSQL repository implementation);
  - password hashing algorithm strategy (current SHA-256 implementation).
